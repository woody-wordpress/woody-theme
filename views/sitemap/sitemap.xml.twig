<?xml version="1.0" encoding="UTF-8"?><?xml-stylesheet type="text/xsl" href="{{ http_host }}/?woody-sitemap=xsl"?>
{% spaceless %}
<urlset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:image="http://www.google.com/schemas/sitemap-image/1.1" xsi:schemaLocation="http://www.sitemaps.org/schemas/sitemap/0.9 http://www.sitemaps.org/schemas/sitemap/0.9/sitemap.xsd http://www.google.com/schemas/sitemap-image/1.1 http://www.google.com/schemas/sitemap-image/1.1/sitemap-image.xsd" xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for url in urls %}
    <url>
        <loc>{{ url.loc}}</loc>
        <lastmod>{{ url.lastmod }}</lastmod>
        {% if url.images|default %}
        {% for image in url.images %}
        <image:image>
            {% if image.loc|default %}
                <image:loc>{{ image.loc }}</image:loc>
            {% endif %}
            {% if image.title|default %}
                <image:title>{{ image.title }}</image:title>
            {% endif %}
            {% if image.caption|default %}
                <image:caption>{{ image.caption }}</image:caption>
            {% endif %}
        </image:image>
        {% endfor %}
        {% endif %}
    </url>
    {% endfor %}
</urlset>
{% endspaceless %}
<!-- XML Sitemap generated by Raccourci Agency -->
